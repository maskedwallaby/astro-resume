---
// Mega
---
<style>
	.multicolored {
		background: linear-gradient(
			to right, 
			var(--color-brand-raspberry-lch), 
			var(--color-brand-yellow-lch)
		);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		padding-block-start: 1rem; /* Why is this needed? */
	}
	.mega, .champion {
		text-transform: uppercase;
	}
	
	.mega {
		font-size: clamp(2rem, 5rem + 4vw, 8rem);
		line-height: 80%;
		letter-spacing: -5%;
		font-variation-settings: "wght" 900;
	}
	.champion {
		font-size: clamp(3rem, 2rem + 1vw, 4rem);
		line-height: 2.5rem;
		letter-spacing: -3%;
		text-indent: clamp(0.75rem, 1vw, 1rem);
		font-variation-settings: "wght" 700;
	}
</style>

<h1 class="multicolored">
	<div class="mega">Chris</div>
	<div class="mega">Weiser</div>
	<div class="champion">Dot Com</div>
</h1>